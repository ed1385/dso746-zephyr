# DSO-746 — технические характеристики

Расчётные, для платы STM32F746G-DISCO без внешнего аналогового тракта.
Правила, заданные для расчёта: **частота выборки ≥ 5 × измеряемой частоты**,
**частота обновления ЦАП ≥ 10 × частоты синтезируемого сигнала**.

---

## 1. Осциллограф

| Параметр | Значение | Откуда |
|---|---|---|
| Каналов | 2, одновременная выборка | ADC1 (PA0) и ADC3 (PF10) от одного события TIM2 TRGO |
| Разрядность | 12 бит | АЦП F746 |
| Макс. частота выборки на канал | **1.80 MSa/s** | ADCCLK 27 МГц / 15 тактов |
| Макс. частота выборки, один канал | 5.40 MSa/s (задел) | тройное чередование по PA0 = ADC123_IN0, не реализовано |
| **Макс. измеряемая частота, 2 канала** | **360 кГц** | 1.8 MSa/s ÷ 5 |
| Макс. измеряемая частота, 1 канал (задел) | 1.08 МГц | 5.4 MSa/s ÷ 5 |
| Диапазон входа (без тракта) | 0…3.3 В | прямо на вход АЦП |
| Диапазон входа (с трактом) | ±5 В; ±50 В с щупом ×10 | делитель ½ + смещение 1.65 В |
| Цена деления | 20 мВ…2 В/дел, ряд 1-2-5 | без тракта осмысленно до 500 мВ/дел (8 дел = 4 В) |
| Разрешение по напряжению | 0.81 мВ/LSB | 3.3 В / 4095 |
| Погрешность АЦП | ≈ ±4 LSB ≈ ±3 мВ | типовая для F7 |
| Развёртка | **20 мкс…100 мс/дел**, ряд 1-2-5, 13 ступеней | нижняя ступень = 336 столбцов на 12 делений при 1.8 MSa/s |
| Длина записи на экран | 336…1344 отсчётов (×1…×4 на столбец) | планировщик acq_plan(): максимальная передискретизация, которую позволяет 1.8 MSa/s |
| Кольцевой буфер | 8192 отсчёта на канал | SRAM без кэша |
| Предзапуск | 0…100 % записи | H.POSITION ±6 делений |
| Отображение | min/max на столбец, послесвечение 0.1 с…∞, XY | выброс в один отсчёт не теряется |

Частота выборки по ступеням развёртки (то, что реально выставляет TIM2):

| мкс/дел | 20 | 50 | 100 | 200 | 500 | 1000 | 2000 | 5000 |
|---|---|---|---|---|---|---|---|---|
| kSa/s | 1400 | 1680 | 1120 | 560 | 224 | 112 | 56 | 22.4 |
| отсчётов/столбец | 1 | 3 | 4 | 4 | 4 | 4 | 4 | 4 |

### Запуск

| Параметр | Значение |
|---|---|
| Тип | программный, по фронту, с гистерезисом и перевзводом |
| Источник | CH1, CH2; EXT на PG6 (D2) — задел |
| Фронт | нарастающий, спадающий, любой |
| Режим | AUTO, NORMAL, SINGLE |
| Гистерезис | 0…160 мВ шагами 8 мВ |
| Джиттер | ±1 отсчёт (0.56 мкс при 1.8 MSa/s) |
| Удержание | до 1 с |

### Измерения

Vpp, Vmax, Vmin, Vavg, Vrms, частота (по переходам через середину с гистерезисом,
период между первым и последним переходом), скважность.

---

## 2. Анализатор спектра

| Параметр | Значение | Откуда |
|---|---|---|
| Длина БПФ | 256…4096 точек | arm_rfft_fast_f32 |
| Окна | прямоугольное, Hann, Hamming, Blackman-Harris, flat-top | с коррекцией усиления окна |
| Полоса обзора | **8 / 32 / 128 / 900 кГц** | частота выборки 16.4k / 65.5k / 262k / 1800 kSa/s, обзор = fs/2 |
| Разрешение по частоте | fs / N: от 4 Гц (8 кГц, 4096 т.) до 440 Гц (900 кГц, 4096 т.) | |
| Время одного БПФ 2048 | ≈ 0.3 мс | Cortex-M7 216 МГц с FPU |
| Динамический диапазон | ≈ 70 дБ | 12 бит + окно; ограничен шумом АЦП |
| Водопад | 84 строки, три палитры | |

---

## 3. Генератор сигналов (PWM-ЦАП на PB4, Arduino D3)

Одно обновление ЦАП = один период ШИМ, то есть частота обновления равна несущей.
Длина таблицы формы подстраивается под частоту: столько точек на период, сколько
позволяет несущая, от 10 до 256.

| Разрядность | Несущая (обновление) | Макс. синус при ≥10 точках/период | Точек на период при 1 кГц |
|---|---|---|---|
| 10 бит | 105.5 кГц | **10.5 кГц** | 105 |
| 8 бит | 421.9 кГц | **42 кГц** | 256 (упор таблицы) |

Прошивка сама выбирает 10 бит, пока частота позволяет, и переходит на 8 бит выше
10.5 кГц.

| Параметр | Значение |
|---|---|
| Формы | синус, меандр, треугольник, пила ↑/↓, импульс, sinc, шум |
| Синус / треугольник / пила | 1 Гц…42 кГц (см. таблицу) |
| Меандр, импульс | прямой ШИМ, до ≈1 МГц; шаг скважности 1 % на 1 МГц, 0.1 % до 100 кГц |
| Размах | 0.05…3.3 Vpp, шаг 50 мВ |
| Смещение | 0…3.3 В |
| Выходной фильтр (внешний) | RC с f_c ≈ 100 кГц, например 1.6 кОм + 1 нФ; для чистого синуса выше 10 кГц — два звена |
| Остаток несущей после фильтра | ≈ −13 дБ на звено при 422 кГц / 100 кГц |
| THD синуса | ≈ 1 % (8 бит), ≈ 0.3 % (10 бит) |
| Выходное сопротивление | = R фильтра; нужен буфер, если нагрузка < 100 кОм |

### Калибратор / жёсткий меандр (PH6, Arduino D6, TIM12_CH1)

2 Гц…2 МГц, скважность 0…100 %, шаг 0.1 % при частоте ≤ 108 кГц.

---

## 4. Интерфейс

| Параметр | Значение |
|---|---|
| Экран | 4.3", 480×272, RGB565, двойная буферизация LTDC |
| Отрисовка луча | 20 кадров/с, карта интенсивности 8 бит на канал, композиция в холст |
| Отклик на касание | ≤ 40 мс: прерывание FT5336 → опрос LVGL 10 мс → кадр LTDC |
| Ускорение | DMA2D для заливок и переносов, по прерыванию |
| Сенсорные цели | ≥ 46 px (9 мм), зазоры ≥ 11 px везде, включая всплывающие панели |

### Два лица прибора

**DEMO** (старт по умолчанию): демонстрационный вид — группа выбирается клавишей
на панели, поле значения перелистывает параметры группы.

**LIVE** (синяя кнопка **B1 USER** на обратной стороне платы, рядом с чёрной B2 RESET; в документации ST — USER & WAKE-UP Button, вывод PI11): рабочий прибор. Клавиша группы открывает
**всплывающую панель настроек** поверх области луча: сетка 3×3 плиток по
102×46 px, по одной на параметр, последняя — DONE. Плитка выбирается касанием,
после чего клавиши − и + и поле значения работают с ней; плитка ВКЛ/ВЫКЛ
переключается сразу. Повторное нажатие клавиши группы, DONE, RUN или MODE
закрывают панель, и луч возвращается. Сбор под панелью не останавливается.
Клавиши − и + при удержании дают автоповтор.

В LIVE открыты все группы прибора: в FFT добавлены CH1/CH2, в GEN — TRIG и TIME.

---

## 5. Ресурсы

FLASH 460 КБ из 1 МБ (44 %), SRAM 121 КБ из 256 (46 %), SDRAM 1.2 МБ из 8.

## 6. Что ограничивает характеристики и как их поднять

- **360 кГц по входу** упираются в один АЦП на канал. Чередование ADC1+ADC2+ADC3 по
  PA0 даёт 5.4 MSa/s и 1 МГц в одноканальном режиме — на этой плате без
  внешних деталей.
- **42 кГц синуса** упираются в несущую ШИМ. Настоящий ЦАП (PA4 разведён только на
  разъём камеры) или внешний ЦАП по SPI подняли бы предел до сотен килогерц.
- **3.3 В по входу** — до появления аналогового тракта из раздела 1.3 плана.
